<div class="bubble {{ item.user == app.user ? 'bg-indigo-200 mx-end' : 'bg-fuschia-200' }}">
	<p class="mb-1 {{ item.user == app.user ? '' : '' }}">{{ item.content }}</p>
</div>
<p class="help fst-italic {{ item.user == app.user ? 'text-end' : 'text-start' }}">{{ item.user.username == app.user.username ? 'moi' : item.user.username }}, il y a
	{{ item.createdAt|time_diff }}</p>
